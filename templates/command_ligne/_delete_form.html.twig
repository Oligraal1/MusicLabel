<form method="post" action="{{ path('command_ligne_delete', {'id': command_ligne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ command_ligne.id) }}">
    <button class="btn">Delete</button>
</form>
